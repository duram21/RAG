# LLM 공급자 (둘 중 쓰는 것만 있어도 동작합니다)
google-genai>=2.0.0        # Gemini
anthropic>=1.0.0           # Claude

# 로컬 임베딩 모델
sentence-transformers>=3.0.0

# 벡터 연산 / 인덱스 저장
numpy>=1.26.0

# .env 파일 로드
python-dotenv>=1.0.0
