# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# 환경변수 (API 키가 들어가므로 절대 커밋 금지)
.env

# 생성된 인덱스 (재생성 가능하므로 커밋하지 않음)
index/

# 임베딩 모델 캐시
.cache/

# 에디터
.vscode/
.idea/
.DS_Store

# 받아온 패치노트 (라이엇 콘텐츠, fetch_patches.py 로 재생성 가능)
# 슬래시가 중간에 있는 패턴은 이 파일 위치 기준으로 고정되므로 ** 를 붙인다.
**/data/patches/

# OP.GG 에서 받아온 데이터 (라이엇/OP.GG 콘텐츠, fetch_items.py 로 재생성)
project/data/items.json

# 생성된 인덱스 (build_index.py 로 재생성)
project/data/index.npy
project/data/index.json
